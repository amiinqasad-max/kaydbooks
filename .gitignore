# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# This project uses the Expo managed workflow (`expo prebuild` generates
# these on demand for a native build/EAS build; they are not hand-edited
# and should not be committed -- see the Phase 1.5 report for the native
# build verification that used them).
/android/
/ios/

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Secrets / environment
.env
.env.*
!.env.example
admin_web/config_service.js
admin_web/config.local.js

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Build artifacts
*.tsbuildinfo
